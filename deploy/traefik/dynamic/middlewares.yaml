http:
  middlewares:
    homehub-security-headers:
      headers:
        contentTypeNosniff: true
        frameDeny: true
        referrerPolicy: no-referrer
        permissionsPolicy: >-
          camera=(), microphone=(), geolocation=(), payment=(), usb=()
        customResponseHeaders:
          Server: ""

    homehub-compress:
      compress: {}
